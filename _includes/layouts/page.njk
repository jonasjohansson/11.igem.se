---
layout: layouts/base.njk
section: page
---

{% if cover %}
	{% hero cover, cover_content %}
{% else %}

<section class="text-layout">
    <h1>{{ title }}<br>{{ preamble | safe }}</h1>
</section>

{% endif %}

{{ content | wrap | safe }}
